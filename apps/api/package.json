{
  "name": "@watchdog/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "dotenv -e ../../.env -- tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test:unit": "echo \"TODO: vitest\"",
    "test:integration": "dotenv -e ../../.env -- vitest run src/routes",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@watchdog/db": "workspace:*",
    "@watchdog/env": "workspace:*",
    "@watchdog/shared-types": "workspace:*",
    "bullmq": "^5.74.1",
    "drizzle-orm": "^0.45.2",
    "fastify": "^5.8.5",
    "ioredis": "^5.10.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "@watchdog/tsconfig": "workspace:*",
    "dotenv-cli": "^8.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.8.2",
    "vitest": "^3.2.4"
  }
}
